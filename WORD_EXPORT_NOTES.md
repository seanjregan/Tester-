# Word Export Notes

I exported all current Markdown documents into `.rtf` files, which open directly in Microsoft Word and can be saved as `.docx` from Word.

## Exported files
- `AGENTS_GONE_WILD_LAUNCH_PLAN.rtf`
- `EPISODE_1_POCKETOS_INTERVIEW_SCRIPT.rtf`
- `HANDLE_CLAIM_CHECKLIST.rtf`
- `NEXT_STEPS_NOW.rtf`
- `X_HEADER_ASSET_SOURCE.rtf`

## Why RTF instead of DOCX
The execution environment does not currently have a DOCX conversion tool installed (for example pandoc/libreoffice/python-docx), so RTF is the most compatible Word-readable format available here.

## Master bundle
- `zAGENTS_GONE_WILD_MASTER.rtf` is a single combined Word-readable file with all current docs and a simple table of contents.
- Open it directly in Microsoft Word (double-click), then optionally Save As `.docx`.
